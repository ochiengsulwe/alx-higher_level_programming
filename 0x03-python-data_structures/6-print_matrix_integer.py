#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    length = len(matrix)

    for i in range(length)
        matrix_row = []
        for row in matrix:
            matrix_row.append(row[i])
        matrix.append(matrix_row)
    return matrix
